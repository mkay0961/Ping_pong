<% if new_object.errors.any? %>
  <div class="box" id="error_explanation">
    <h2>
      <%= pluralize(new_object.errors.count, "error") %> prohibited this user from being saved!
    </h2>
    <ul>
      <% new_object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
