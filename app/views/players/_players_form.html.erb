
<% if @player.errors.any? %>
 <div class="box" id=“error_explanation”>
   <h2>
     <%= pluralize(@player.errors.count, "error") %> prohibited this player from being saved!
   </h2>
   <ul>
     <% @player.errors.full_messages.each do |msg| %>
       <li><%= msg %></li>
     <% end %>
   </ul>
 </div>
<% end %>

<div class="box">

  <%= form_for @player, :html => {:class => "ui form"} do |f| %>
  <div class="two fields">
    <div class="field">
      <%= f.label :first_name %>
      <%= f.text_field :first_name %>
    </div><div class="field">
      <%= f.label :last_name %>
      <%= f.text_field :last_name %>
    </div>
  </div>
  <div class="two fields">
    <div class="field">
      <%= f.label :nickname %>
      <%= f.text_field :nickname %>
    </div><div class="field">
      <%= f.label :img_url %>
      <%= f.text_field :img_url %>
    </div>
  </div>
      <%= f.submit "Submit", class: "circular ui inverted button"  %>
  <% end %>
</div>
